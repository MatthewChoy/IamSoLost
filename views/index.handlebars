<div class= "container">
    <div class ="row">
      <div class="col-xs-12 text-center" id="top-bar">
          <h1>Keto Burger Eatery</h1>
          </div>
        <div class= "col-xs-6 text-center" class="task">
            <div class="row">
                {{#each burger_data}}
                {{#unless this.devoured}}
                <div class="col-xs-9 text-center">
                <pre>{{this.id}}. {{this.burger_name}}</pre>    
                </div>
                <div class="col-xs-3 text-center">
                    <form method="POST" action="/burgers/update?_method=PUT"
                    class="button-size">
                    <input type="hidden" name="burger_id" type="text"
                    value={{this.id}}>
                    <br>
                    <button type="submit" class="btn btn-default">EAT IT!!!</button>
                    </form>
                </div>
                {{/unless}}
                {{/each}}
            </div>
        </div>
        <div class="col-xs-6 text-center" class="task">
            {{#each burger_data}}
            {{#if this.devoured}}
            <input class="form-control" type="text" placeholder=
            "{{this.id}}.{{this.burger_name}}" readonly>
            {{/if}}
            {{/each}}
        </div>
    </div>
</div>
<div class="container">
    <div class="row">
        <div class="col-xsp12 text-center enter_section">
            <form action="/burgers/create" method="POST">
            <input type="text" name="burger_name" class="form-control"
            value="" id="enter_text">
            <button type="submit" class="btn btn-primary" id="text-enter-button">
                SUBMIT</button>
                <p>Type in the meat you want to eat!!!</p>
            </form>
        
<h1>PICK A BURGER</h1>

<h2>BUNLESS BURGERS ARE KING</h2>

<ul>
  {{#each burger_name}}
    {{#unless burger_id}}
      {{> burger_name devoured=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Cats that are sleepy!</h2>

<ul>
  {{#each burger_data}}
    {{#if burger_id}}
      {{> burger_name devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="burger-class">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="burger-name" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy"></label><br>
    <input type="eat" name="eaten" value="1" checked> Eaten!<br>
    <input type="noEat" name="noEaten" value="0"> Not Eaten
  </div>

  <button type="submit">Add Burger</button>
</form> --}}
